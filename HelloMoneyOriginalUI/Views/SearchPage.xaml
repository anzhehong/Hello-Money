<Page
    x:Class="NavigationMenuSample.Views.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NavigationMenuSample.Views"
    xmlns:ctl="using:NavigationMenuSample.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid  Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"    EntranceNavigationTransitionInfo.IsTargetElement="True" Margin="10">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="800" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="searchStackPanel.Orientation" Value="Horizontal"/>
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <StackPanel>
            <TextBlock Text="" Style="{StaticResource SampleHeaderTextStyle}" />
        </StackPanel>

        <Grid x:Name="ContentPanel" Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="Auto"></RowDefinition>
            </Grid.RowDefinitions>
          
            <!--记账记录列表头项目-->
            <Grid Grid.Row="0" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"></ColumnDefinition>
                    <ColumnDefinition Width="2*"></ColumnDefinition>
                    <ColumnDefinition Width="5*"></ColumnDefinition>
                    <ColumnDefinition Width="3*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Background="Gray">
                    <TextBlock Text="Type" Foreground="White" />
                </Border>
                <Border Grid.Column="1" Background="Gray">
                    <TextBlock Text="Amount" Foreground="White"/>
                </Border>
                <Border Grid.Column="2" Background="Gray">
                    <TextBlock Text="Remark" Foreground="White"/>
                </Border>
                <Border Grid.Column="3" Background="Gray">
                    <TextBlock Text="category" Foreground="White"/>
                </Border>
            </Grid>
            <!--记账记录数据绑定列表-->
            <ListView Grid.Row="1" x:Name="listReport" >
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid  HorizontalAlignment="Stretch">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"></RowDefinition>
                                <RowDefinition Height="Auto"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*"></ColumnDefinition>
                                <ColumnDefinition Width="2*"></ColumnDefinition>
                                <ColumnDefinition Width="5*"></ColumnDefinition>
                                <ColumnDefinition Width="3*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <!--<TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding Type,  Converter={StaticResource VoucherTypeConverter}}" />
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Money}"/>
                            <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding Desc,  Converter={StaticResource VoucherDescConverter}}"/>
                            <TextBlock Grid.Row="0" Grid.Column="3" Text="{Binding Category}"/>
                            <TextBlock Grid.Row="1" Grid.ColumnSpan="4" Text="{Binding DT}" HorizontalAlignment="Right"/>-->
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <!--查询条件-->
            <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                    <RowDefinition Height="auto"></RowDefinition>
                </Grid.RowDefinitions>

                <DatePicker x:Name="DatePickerBegin"  Grid.Row="0" HorizontalAlignment="Center" />

                <TextBlock Foreground="White" Text="Till" Grid.Row="1"/>
                <DatePicker x:Name="DatePickerEnd" Grid.Row="1" HorizontalAlignment="Center" />

                <Grid  Grid.Row="2">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"></ColumnDefinition>
                        <ColumnDefinition Width="8*"></ColumnDefinition>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="search:" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <TextBox x:Name="keyWords" Grid.Column="1" />
                </Grid>

            </Grid>
        </Grid>
        
    </Grid>
    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Icon="Zoom" Label="Search" />

        </CommandBar>
    </Page.BottomAppBar>
</Page>
